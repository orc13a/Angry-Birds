// +----------------------------------+
// |        -- PLAN / TO DO --        |
// +----------------------------------+
// [ NOT DONE ]  Lav slangebøsse og den affyret fugls skal have en flyve bane

int groundHeight = 30;

void setup() {
  fullScreen();
  frameRate(120);
  rectMode(CENTER);
}

void draw() {
  clear();
  background(255);
  
  
}
